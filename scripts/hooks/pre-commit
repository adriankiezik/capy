#!/bin/sh

set -eu

repo_root="$(git rev-parse --show-toplevel)"

"$repo_root/scripts/check-agent-links.sh"

# Run cargo fmt and fail the commit if code isn't formatted.
exec cargo fmt --manifest-path "$repo_root/Cargo.toml" --all -- --check
