#!/bin/sh

set -eu

repo_root="$(git rev-parse --show-toplevel)"

"$repo_root/scripts/check-agent-links.sh"

# Auto-format code and re-stage changed files before committing.
cargo fmt --manifest-path "$repo_root/Cargo.toml" --all
git diff --name-only --diff-filter=d | xargs -r git add
