Never do when not asked to:
- create tests
- add comments
- create docs/md files
- create python files
- do not add any logs (unless temporarily needed for the task - remove afterwards)

Important:
- each directory module should have mod.rs which should contain only exports
- modules should define their errors in error.rs, config in config.rs, tests in tests.rs, and CPU benches in bench.rs
